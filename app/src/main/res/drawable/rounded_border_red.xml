<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/errorColor" />
    <stroke
        android:width="2dp"
        android:color="@color/errorColor"
        />
    <corners android:radius="10dp" />
</shape>
